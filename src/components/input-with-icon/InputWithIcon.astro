---
import "./InputWithIcon.scss";


interface Props{
    label: string;
    placeholder: string;
    name: string;
    required?: boolean;
    type?: any;
}



const {label, placeholder, name, type = "text", required} = Astro.props;
---
<label class="input input-withicon">
    <span>{label}</span>
    <div class="input-box">
        <slot />
        <input class="form-control" type={type} name={name} placeholder={placeholder} required={required} />
    </div>
</label>